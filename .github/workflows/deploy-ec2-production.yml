name: Deploy to EC2 Production

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to deploy (branch, tag, or commit SHA)
        required: false
        default: main
        type: string
      image_tag:
        description: Docker image tag to publish. Defaults to deploy-<short-sha>.
        required: false
        default: ""
        type: string
      build_api:
        description: Also build and deploy the API image. Leave false for UI-only changes.
        required: false
        default: false
        type: boolean

permissions:
  contents: read
  packages: write

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    environment:
      name: production

    steps:
      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Checkout deploy ref
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || 'main' }}
          submodules: true

      - name: Validate required configuration
        env:
          EC2_HOST: ${{ vars.EC2_HOST }}
          EC2_USER: ${{ vars.EC2_USER }}
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH }}
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
          OSS_JWT_SECRET: ${{ secrets.OSS_JWT_SECRET }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          set -euo pipefail

          missing=""
          for name in EC2_HOST EC2_USER DEPLOY_PATH SSH_PRIVATE_KEY REDIS_URL OSS_JWT_SECRET DOCKERHUB_USERNAME DOCKERHUB_TOKEN; do
            if [ -z "${!name:-}" ]; then
              missing="$missing $name"
            fi
          done

          if [ -n "$missing" ]; then
            echo "Missing required GitHub Environment vars/secrets:$missing"
            exit 1
          fi

      - name: Compute image tags
        id: image
        env:
          INPUT_IMAGE_TAG: ${{ github.event.inputs.image_tag }}
          BUILD_API: ${{ github.event.inputs.build_api || 'false' }}
        run: |
          set -euo pipefail
          short_sha="$(git rev-parse --short=12 HEAD)"
          image_tag="${INPUT_IMAGE_TAG:-}"

          if [ -z "$image_tag" ]; then
            image_tag="deploy-${short_sha}"
          fi

          api_image_tag="latest"
          if [ "$BUILD_API" = "true" ]; then
            api_image_tag="$image_tag"
          fi

          echo "image_tag=$image_tag" >> "$GITHUB_OUTPUT"
          echo "ui_image_tag=$image_tag" >> "$GITHUB_OUTPUT"
          echo "api_image_tag=$api_image_tag" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push UI image
        env:
          REGISTRY: ${{ vars.REGISTRY }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          UI_IMAGE_TAG: ${{ steps.image.outputs.ui_image_tag }}
        run: |
          set -euo pipefail
          registry="${REGISTRY:-$DOCKERHUB_USERNAME}"

          docker buildx build \
            --platform linux/amd64 \
            -f ui/Dockerfile \
            -t "$registry/dograh-ui:$UI_IMAGE_TAG" \
            --push \
            .

      - name: Build and push API image
        if: ${{ github.event.inputs.build_api == 'true' }}
        env:
          REGISTRY: ${{ vars.REGISTRY }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          API_IMAGE_TAG: ${{ steps.image.outputs.api_image_tag }}
        run: |
          set -euo pipefail
          registry="${REGISTRY:-$DOCKERHUB_USERNAME}"

          docker buildx build \
            --platform linux/amd64 \
            -f api/Dockerfile \
            -t "$registry/dograh-api:$API_IMAGE_TAG" \
            --push \
            .

      - name: Setup SSH key
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' '${{ secrets.EC2_SSH_PRIVATE_KEY }}' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Trust EC2 host key
        run: |
          set -euo pipefail
          ssh-keyscan -H '${{ vars.EC2_HOST }}' >> ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: Deploy stack on EC2
        env:
          DEPLOY_REF: ${{ github.event.inputs.ref || 'main' }}
          API_IMAGE_TAG: ${{ steps.image.outputs.api_image_tag }}
          UI_IMAGE_TAG: ${{ steps.image.outputs.ui_image_tag }}
          EC2_HOST: ${{ vars.EC2_HOST }}
          EC2_USER: ${{ vars.EC2_USER }}
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
          OSS_JWT_SECRET: ${{ secrets.OSS_JWT_SECRET }}
          REGISTRY: ${{ vars.REGISTRY }}
          BACKEND_API_ENDPOINT: ${{ vars.BACKEND_API_ENDPOINT }}
          BACKEND_URL: ${{ vars.BACKEND_URL }}
          MINIO_PUBLIC_ENDPOINT: ${{ vars.MINIO_PUBLIC_ENDPOINT }}
          TURN_HOST: ${{ vars.TURN_HOST }}
          TURN_SECRET: ${{ secrets.TURN_SECRET }}
          FORCE_TURN_RELAY: ${{ vars.FORCE_TURN_RELAY }}
          ENABLE_TELEMETRY: ${{ vars.ENABLE_TELEMETRY }}
          FASTAPI_WORKERS: ${{ vars.FASTAPI_WORKERS }}
        run: |
          set -euo pipefail

          ssh "$EC2_USER@$EC2_HOST" \
            "set -euo pipefail; \
            cd '$DEPLOY_PATH'; \
            git fetch --all --tags; \
            git checkout '$DEPLOY_REF'; \
            git pull --ff-only origin '$DEPLOY_REF'; \
            REDIS_URL='$REDIS_URL' \
            OSS_JWT_SECRET='$OSS_JWT_SECRET' \
            API_IMAGE_TAG='$API_IMAGE_TAG' \
            UI_IMAGE_TAG='$UI_IMAGE_TAG' \
            REGISTRY='${REGISTRY:-}' \
            BACKEND_API_ENDPOINT='${BACKEND_API_ENDPOINT:-}' \
            BACKEND_URL='${BACKEND_URL:-}' \
            MINIO_PUBLIC_ENDPOINT='${MINIO_PUBLIC_ENDPOINT:-}' \
            TURN_HOST='${TURN_HOST:-}' \
            TURN_SECRET='${TURN_SECRET:-}' \
            FORCE_TURN_RELAY='${FORCE_TURN_RELAY:-}' \
            ENABLE_TELEMETRY='${ENABLE_TELEMETRY:-}' \
            FASTAPI_WORKERS='${FASTAPI_WORKERS:-}' \
            docker compose --profile remote pull api ui; \
            REDIS_URL='$REDIS_URL' \
            OSS_JWT_SECRET='$OSS_JWT_SECRET' \
            API_IMAGE_TAG='$API_IMAGE_TAG' \
            UI_IMAGE_TAG='$UI_IMAGE_TAG' \
            REGISTRY='${REGISTRY:-}' \
            BACKEND_API_ENDPOINT='${BACKEND_API_ENDPOINT:-}' \
            BACKEND_URL='${BACKEND_URL:-}' \
            MINIO_PUBLIC_ENDPOINT='${MINIO_PUBLIC_ENDPOINT:-}' \
            TURN_HOST='${TURN_HOST:-}' \
            TURN_SECRET='${TURN_SECRET:-}' \
            FORCE_TURN_RELAY='${FORCE_TURN_RELAY:-}' \
            ENABLE_TELEMETRY='${ENABLE_TELEMETRY:-}' \
            FASTAPI_WORKERS='${FASTAPI_WORKERS:-}' \
            docker compose --profile remote up -d --remove-orphans; \
            docker compose --profile remote ps"

      - name: Smoke check API health
        env:
          EC2_HOST: ${{ vars.EC2_HOST }}
          EC2_USER: ${{ vars.EC2_USER }}
        run: |
          set -euo pipefail
          ssh "$EC2_USER@$EC2_HOST" "curl -fsS http://localhost:8000/api/v1/health > /dev/null"

      - name: Cleanup SSH key
        if: always()
        run: |
          rm -f ~/.ssh/id_rsa
